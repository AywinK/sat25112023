const FooterNavigation = () => {
  return (
    <nav
      style={{
        display: "flex",
        flexDirection: "column",
      }}
    >
      <a href="#">Accounts</a>
      <a href="#">Lists</a>
      <a href="#">Orders</a>
      <a href="#">Returns</a>
    </nav>
  );
};

export { FooterNavigation };
